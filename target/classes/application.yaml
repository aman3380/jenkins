spring:
  application:
    name: tasktracker

  datasource:
    url: jdbc:mysql://localhost:3306/task
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081